import { extendTheme } from '@chakra-ui/react'

const theme = extendTheme({
    colors: {
        dark: "#222831",
        gray: "#393E46",
        accent: "#00ADB5",
        light: "#EEEEEE"
    }
})

export default theme;